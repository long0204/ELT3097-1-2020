<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.alarmy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Settingg"
            android:label="@string/title_activity_settingg"></activity>
        <activity
            android:name=".mhsetting"
            android:label="@string/title_activity_mhsetting" />
        <activity
            android:name=".manhinhcaidat"
            android:label="@string/title_activity_manhinhcaidat" />
        <activity android:name=".Setting" />
        <activity android:name=".plus" />
        <activity android:name=".checktime1" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name=".AlarmReceiver" />

        <service android:name=".Music" />
    </application>

</manifest>